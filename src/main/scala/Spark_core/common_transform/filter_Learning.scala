package Spark_core.common_transform

object filter_Learning {
  /**
   * filter(func)
   * Return a new dataset formed by selecting those elements of the source on which func returns true.
   * eg:filter((a,b)=>a==b)
   */
  def filterLearning(): Unit ={

  }
}
