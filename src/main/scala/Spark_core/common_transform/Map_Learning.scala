package Spark_core.common_transform

object Map_Learning {
  /**
   * map(func)
   * Return a new distributed dataset formed by passing each element of the source through a function func.
   * eg:map((a,b)=>a+b)
   */
  def mapLearning(): Unit ={

  }
}
