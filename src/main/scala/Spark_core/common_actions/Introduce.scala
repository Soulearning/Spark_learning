package Spark_core.common_actions

object Introduce {
  /**
   * RDDs support two types of operations: transformations and actions
   * actions：which return a value to the driver program after running a computation on the dataset.
   * For example,reduce is an action that aggregates all the elements of the RDD using some function
   * and returns the final result to the driver program (although there is also a parallel reduceByKey
   * that returns a distributed dataset).
   */
}
